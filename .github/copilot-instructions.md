Use `AGENTS.md` as the repo entrypoint.
If `AI.md` exists, read it too.
Prefer repo docs, `README.md`, and the current operational notes over assumptions.
Do not commit secrets or local-only memory files.
